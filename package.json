{
  "name": "kr-100-mountains",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "대한민국 100대 명산 위키 — 산림청 · 블랙야크(BAC) 통합 인터랙티브 지도/위키",
  "scripts": {
    "registry": "node scripts/build-registry.mjs",
    "build:data": "node scripts/build-data.mjs",
    "dev": "vite",
    "build": "npm run build:data && vite build",
    "preview": "vite preview",
    "test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
